[gd_scene load_steps=2 format=3 uid="uid://dop4soleb5bcq"]

[sub_resource type="GDScript" id="GDScript_8mcrv"]
script/source = "extends Node3D

@export var weapon_to_fire : PackedScene
@export var fire_rate : float
@export var fire_positions : Array[Vector3]
@export var fire_rotations : Array[Vector3]
@export var delay_between_weapons : float

var timer : float = 0.0

# Called when the node enters the scene tree for the first time.
func _ready() -> void:
	pass # Replace with function body.


# Called every frame. 'delta' is the elapsed time since the previous frame.
func _process(delta: float) -> void:
	timer += delta
	if timer > fire_rate:
		Fire()
		timer = 0.0

func Fire():
	pass
"

[node name="WeaponLauncher" type="Node3D"]
script = SubResource("GDScript_8mcrv")

[node name="MeshInstance3D" type="MeshInstance3D" parent="."]
